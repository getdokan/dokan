/**
 * Basically this file useed to contain the dependencies of packages.json and this
 * file used to load in a lot of places through dependencies. Now we don't load dependencies like that.
 * Now this file can be used for some common functionalities that we load every where.
 *
 * @since DOKAN_SINCE
 */
