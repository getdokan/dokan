import { Vendor } from '@dokan/definitions/dokan-vendors';

export interface TabSectionProps {
    vendor: Vendor;
}
const TabSections = ( { vendor }: TabSectionProps ) => {};
export default TabSections;
